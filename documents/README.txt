Place your PDF documents in this directory to enable the PDF viewer functionality.

The application will automatically detect and make PDF citations clickable when they are mentioned in GraphRAG responses.

Supported file formats:
- PDF (.pdf)

Note: This directory is mounted as read-only in Docker containers for security.